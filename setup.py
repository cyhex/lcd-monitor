from setuptools import setup, find_packages

setup(
    name='lcd-monitor',
    version='0.01',
    packages=['lcdm'],
    url='https://github.com/cyhex/lcd-monitor',
    install_requires=[
        "requests"
    ],
)
